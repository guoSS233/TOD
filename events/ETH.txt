add_namespace = ETH_event

country_event = {
    id = ETH_event.1
	title = ETH_event.1.t
	desc = ETH_event.1.d

    fire_only_once = yes
	is_triggered_only = yes

    trigger = {
       date > 1936.7.1
    }

    option = {
        name = ETH_event.1.a
        set_global_flag = global_flag_IEA_ETH_war
        declare_war_on = { target = ETH type = annex_everything }
    }
}

country_event = {
    id = ETH_event.2
	title = ETH_event.2.t
	desc = ETH_event.2.d

    fire_only_once = yes
	is_triggered_only = yes

    trigger = {
        IEA = {
            has_war_with = ETH
        }
        OR = {
            ETH = {
                surrender_progress > 30
            }
            date > 1936.11.1
        }
    }

    option = {
        name = ETH_event.2.a
        set_global_flag = global_flag_IEA_ETH_war
        declare_war_on = { target = IEA type = annex_everything }
    }
}
