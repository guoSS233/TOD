on_actions = {
	on_daily = {
		effect = {
			CHL = {
				country_event = { id = TOD_CHL.1 }
				country_event = { id = TOD_CHL.4 }
				country_event = { id = TOD_CHL.100 }
				country_event = { id = TOD_CHL.101 }
			}
		}
	}
}