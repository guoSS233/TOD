# Compatibility effects used by the TOD event scripts.
# The effect is called inside the recipient country's scope; PREV is the
# source country scope in the existing event chains.
transfer_technology = {
	inherit_technology = PREV
}
