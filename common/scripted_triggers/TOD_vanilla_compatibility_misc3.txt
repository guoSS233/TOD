# Vanilla compatibility definitions ported from NOR_scripted_triggers.txt.

NOR_AAT = { # Trigger to check if playing as Norway while having AAT
	original_tag = NOR
	has_dlc = "Arms Against Tyranny"
}
