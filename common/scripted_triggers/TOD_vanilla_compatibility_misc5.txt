# Vanilla compatibility definitions ported from PHI_scripted_triggers.txt.

PHI_SEA = { # Trigger to check if playing as Philippines while having SEA
	original_tag = PHI
	has_dlc = "No Compromise, No Surrender"
}
